public class maths {
    public static void main(String[] args) {
        //maths
        //5, 6
        //random
        System.out.println(Math.max(5,6));
        System.out.println(Math.min(5,6));

        // Math random prints random values
        System.out.println((int)(Math.random()*100));
        // Here we multiply it too 100 cause the default limit of the random values are too short.
    }
    
}
